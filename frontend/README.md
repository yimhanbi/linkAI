# Frontend (React + Vite)

## Setup

```bash
cd frontend
npm install
```

## Environment variables

Create `frontend/.env.local` (not committed) if needed.

## Run

```bash
cd frontend
npm run dev
```

## Build

```bash
cd frontend
npm run build
```

